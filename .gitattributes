# Git LFS — track large PDF lesson planners as large files.
# Scoped to english/ (60+MB scans that need it); math/ scans are small
# enough (<25MB each) to commit as regular blobs, which this session's
# environment can push directly — LFS uploads are blocked here.
docs/lesson-plans/english/**/*.pdf filter=lfs diff=lfs merge=lfs -text
